import Link from "next/link";
import { FolderOpen,FilePlus2,Clock3 } from "lucide-react";
import type { Role } from "@/lib/types";
export function DocumentSectionNav({role}:{role:Role}){const links=[{href:"/documentos",label:role==="gerente"?"Historial":"Mis documentos",icon:FolderOpen},...(role==="coordinador"?[{href:"/documentos/nuevo",label:"Nuevo documento",icon:FilePlus2}]:[]),...(role==="gerente"?[{href:"/documentos/pendientes",label:"Pendientes",icon:Clock3}]:[])];return <nav aria-label="Secciones de documentos" className="mb-5 flex gap-1 overflow-x-auto border-b border-[#DCE3EC]">{links.map(({href,label,icon:Icon})=><Link key={href} href={href} className="flex shrink-0 items-center gap-2 border-b-2 border-transparent px-3 py-2.5 text-sm font-medium text-[#607089] hover:border-[#B9C9DF] hover:text-[#174EA6]"><Icon size={16}/>{label}</Link>)}</nav>}
