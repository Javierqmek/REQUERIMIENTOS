"use client";
import { LoaderCircle, LogOut, X } from "lucide-react";
export function ConfirmDialog({open,busy,onCancel,onConfirm}:{open:boolean;busy:boolean;onCancel:()=>void;onConfirm:()=>void}){if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4" role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target&&!busy)onCancel()}}><section role="dialog" aria-modal="true" aria-labelledby="logout-title" className="card w-full max-w-sm p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><span className="grid h-12 w-12 place-items-center rounded-xl bg-red-50 text-red-700"><LogOut size={23}/></span><button className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Cerrar diálogo" disabled={busy} onClick={onCancel}><X size={20}/></button></div><h2 id="logout-title" className="mt-5 text-xl font-extrabold text-slate-900">¿Deseas cerrar tu sesión?</h2><p className="mt-2 text-sm text-slate-500">Tendrás que volver a ingresar tus credenciales para continuar.</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><button className="btn btn-secondary" disabled={busy} onClick={onCancel}>Cancelar</button><button className="btn btn-danger" disabled={busy} onClick={onConfirm}>{busy&&<LoaderCircle className="animate-spin" size={18}/>}Sí, cerrar sesión</button></div></section></div>}
